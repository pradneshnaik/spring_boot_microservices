spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/course-services?useSSL=false
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: course-services
server:
  port: 9002

eureka:
  instance:
    hostname: localhost